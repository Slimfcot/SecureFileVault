<Window x:Class="SecureFileVault.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Secure File Vault - Dashboard"
        Height="550"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FB">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="White"
                CornerRadius="12"
                Padding="20"
                Margin="0,0,0,15">

            <StackPanel>
                <TextBlock Text="🔐 Secure File Vault"
                           FontSize="28"
                           FontWeight="Bold"/>

                <TextBlock x:Name="UsernameTextBlock"
                           FontSize="18"
                           Margin="0,8,0,0"/>

                <TextBlock x:Name="FileCountTextBlock"
                           FontSize="14"
                           Foreground="Gray"
                           Margin="0,5,0,0"/>
            </StackPanel>

        </Border>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">

            <Button Content="📁 Upload File"
                    Width="140"
                    Height="40"
                    Margin="0,0,10,0"
                    FontWeight="SemiBold"
                    Click="UploadFileButton_Click"/>

            <Button Content="🔄 Refresh"
                    Width="120"
                    Height="40"
                    Margin="0,0,10,0"
                    FontWeight="SemiBold"
                    Click="RefreshButton_Click"/>

            <Button Content="📜 Audit Logs"
                    Width="140"
                    Height="40"
                    Margin="0,0,10,0"
                    FontWeight="SemiBold"
                    Click="AuditLogsButton_Click"/>

            <Button Content="🚪 Logout"
                    Width="120"
                    Height="40"
                    FontWeight="SemiBold"
                    Click="LogoutButton_Click"/>
        </StackPanel>

        <!-- Files Grid -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="12"
                Padding="10">

            <DataGrid x:Name="FilesDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      RowHeight="35"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F3F6FA">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="File Name"
                                        Binding="{Binding OriginalFileName}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Extension"
                                        Binding="{Binding FileExtension}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Size (bytes)"
                                        Binding="{Binding FileSize}"
                                        Width="140"/>

                    <DataGridTextColumn Header="Uploaded At"
                                        Binding="{Binding UploadedAt}"
                                        Width="180"/>

                    <DataGridTemplateColumn Header="Actions"
                                            Width="220">

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <StackPanel Orientation="Horizontal">

                                    <Button Content="⬇ Download"
                                            Margin="0,0,5,0"
                                            Click="DownloadFileButton_Click"/>

                                    <Button Content="🗑 Delete"
                                            Click="DeleteFileButton_Click"/>

                                </StackPanel>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>
</Window>